import React from 'react'

import Login from 'dashboard/components/Login'

// TODO: Check to see if user is logged in, and render Tokens page instead

export default function App(props) {
  return (
    <div>
      <Login />
    </div>
  )
}
